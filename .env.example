DB_PASSWORD="CHANGE_ME_strong_random_password"
DATABASE_URL="postgresql://nfc_user:CHANGE_ME@postgres:5432/nfc_tracker"
NEXTAUTH_SECRET="CHANGE_ME_min_32_random_chars"
NEXTAUTH_URL="http://twojenfc.pl"
ADMIN_EMAIL="admin"
ADMIN_PASSWORD="CHANGE_ME_admin_password"
NODE_ENV="production"
UPLOAD_DIR="/app/public/uploads"
MAX_FILE_SIZE_MB=500
# P0 Telemetry: HMAC-SHA256 key for privacy-safe IP hashing (ipPrefix/ipVersion fields)
# Generate: openssl rand -hex 32
IP_HASH_SECRET="CHANGE_ME_random_64_chars_for_hmac_ip_hashing"
# Legacy IP hash salt (used for existing ipHash column, backward compat)
# IP_HASH_SALT="nfc-tracker-salt-2024"
